version: '3.7'
services:

  alpine:
    image: alpine:latest
    working_dir: /code
    volumes: 
      - ./src:/code

  ubuntu:
    image: ubuntu:latest
    working_dir: /code
    volumes: 
      - ./src:/code

  debian:
    image: debian:latest
    working_dir: /code
    volumes: 
      - ./src:/code

  fedora:
    image: fedora:latest
    working_dir: /code
    volumes: 
      - ./src:/code
